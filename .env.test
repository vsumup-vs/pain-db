# Test Database URL - Local PostgreSQL only
# DATABASE_URL will be set by GitHub Actions
NODE_ENV=test

# Server
PORT=3001

# API Configuration
API_VERSION=v1
API_PREFIX=/api

# Explicitly disable Prisma Accelerate
PRISMA_ACCELERATE_DISABLE=true
PRISMA_DISABLE_ACCELERATE=true